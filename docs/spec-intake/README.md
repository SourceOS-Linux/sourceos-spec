# Spec Intake Register — 2026-07-31

Upstream capture of the spec-corpus intake index (so the work-plan is not lost
if the local staging dir goes away). The **raw corpus** (312 files, ~13 MB,
some sensitive) stays local at `~/dev/spec-intake/2026-07-31/` and mirrors the
user's own `~/Documents`; it is intentionally NOT committed here pending an
explicit decision on a private target.

- `PROGRAM_SPEC_REGISTER.md` — the work plan: 6 clusters → target repos + order.
- `MANIFEST.sha256` — integrity hashes for all captured files.
- `INVENTORY_SUMMARY.txt` — file counts per source/zip.
