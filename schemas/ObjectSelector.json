{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.srcos.ai/v2/ObjectSelector.json",
  "title": "ObjectSelector",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "objectType"
  ],
  "properties": {
    "objectType": {
      "enum": [
        "dataset",
        "field",
        "asset",
        "run",
        "provenance",
        "schema",
        "agreement",
        "any"
      ],
      "description": "The resource type this selector matches (use 'any' to match all types)."
    },
    "match": {
      "type": "object",
      "additionalProperties": true,
      "description": "Attribute predicate map used to filter matching objects."
    }
  },
  "description": "A policy scope selector that matches target objects by their type and attribute predicates, used in Policy.scope to define which resources the policy governs."
}