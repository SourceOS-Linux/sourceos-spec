{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.srcos.ai/v2/SubjectContext.json",
  "title": "SubjectContext",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "subjectId",
    "kind"
  ],
  "properties": {
    "subjectId": {
      "type": "string",
      "description": "URN of the requesting subject."
    },
    "kind": {
      "enum": [
        "user",
        "service",
        "app",
        "workload"
      ],
      "description": "Category of the requesting subject."
    },
    "roles": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of role names currently held by this subject (e.g., 'owner', 'analyst')."
    },
    "attributes": {
      "type": "object",
      "additionalProperties": true,
      "description": "Additional key/value attributes for fine-grained policy conditions."
    }
  },
  "description": "The subject (requestor) side of a policy evaluation request, identifying the requesting entity by URN, kind, assigned roles, and arbitrary attributes."
}