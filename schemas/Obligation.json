{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.srcos.ai/v2/Obligation.json",
  "title": "Obligation",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "when"
  ],
  "properties": {
    "name": {
      "enum": [
        "obfuscate_before_export",
        "log_access",
        "retain_provenance",
        "mask_fields",
        "aggregate_only",
        "attest_container"
      ],
      "description": "Named obligation to enforce."
    },
    "when": {
      "enum": [
        "pre",
        "post",
        "runtime"
      ],
      "description": "When the obligation must be fulfilled relative to the access grant."
    },
    "params": {
      "type": "object",
      "additionalProperties": true,
      "description": "Obligation-specific parameters (e.g., workload URN for obfuscation obligations)."
    }
  },
  "description": "An enforceable action that must be performed before (pre) or after (post) granting access, or continuously at runtime \u2014 such as obfuscation, access logging, or provenance capture."
}