{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.srcos.ai/v2/SubjectSelector.json",
  "title": "SubjectSelector",
  "description": "A match clause used in a Policy scope to select which subject kinds and attributes the policy applies to.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "kind"
  ],
  "properties": {
    "kind": {
      "enum": [
        "user",
        "service",
        "app",
        "workload",
        "any"
      ],
      "description": "The subject kind this selector matches: user, service, app, workload, or any."
    },
    "match": {
      "type": "object",
      "additionalProperties": true,
      "description": "Attribute match criteria. Keys and values are defined by the policy engine implementation."
    }
  }
}
