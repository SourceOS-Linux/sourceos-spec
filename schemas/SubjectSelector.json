{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.srcos.ai/v2/SubjectSelector.json",
  "title": "SubjectSelector",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "kind"
  ],
  "properties": {
    "kind": {
      "enum": [
        "user",
        "service",
        "app",
        "workload",
        "any"
      ],
      "description": "The subject kind this selector matches (use 'any' to match all kinds)."
    },
    "match": {
      "type": "object",
      "additionalProperties": true,
      "description": "Attribute predicate map used to filter matching subjects."
    }
  },
  "description": "A policy scope selector that matches requesting subjects by their kind and attribute predicates, used in Policy.scope to define who the policy applies to."
}