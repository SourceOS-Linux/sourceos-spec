{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.srcos.ai/v2/AuthorityLink.json",
  "title": "AuthorityLink",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "system",
    "value"
  ],
  "properties": {
    "system": {
      "type": "string",
      "description": "Identifier for the authority system (e.g., 'ldap', 'oauth2', 'wikidata')."
    },
    "value": {
      "type": "string",
      "description": "Authority-system-specific identifier or code for this entity."
    },
    "url": {
      "type": [
        "string",
        "null"
      ],
      "format": "uri",
      "description": "Optional canonical URL to the authority record."
    }
  },
  "description": "A reference to an external authority or identity system that corroborates the identity, classification, or ontology alignment of an entity (e.g., LDAP DN, OAuth issuer, taxonomy URI)."
}