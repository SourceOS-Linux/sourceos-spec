{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.srcos.ai/v2/DataRef.json",
  "title": "DataRef",
  "description": "A typed pointer to a data entity (dataset, asset, stream topic, or file) with optional field-path restrictions.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "refType",
    "id"
  ],
  "properties": {
    "refType": {
      "enum": [
        "dataset",
        "asset",
        "streamTopic",
        "file"
      ],
      "description": "The type of data entity referenced: dataset, asset, streamTopic, or file."
    },
    "id": {
      "type": "string",
      "description": "URN or identifier of the referenced entity."
    },
    "fieldPaths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional JSONPath expressions restricting which fields of the entity are in scope (e.g. \"$.patient.dateOfBirth\")."
    }
  }
}
